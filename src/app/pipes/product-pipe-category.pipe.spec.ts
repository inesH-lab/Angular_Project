import { ProductPipeCategoryPipe } from './product-pipe-category.pipe';

describe('ProductPipeCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductPipeCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
