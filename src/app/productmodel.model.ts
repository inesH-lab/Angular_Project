export class Productmodel {

    constructor(public name:string,  public detail:string, public price:number, public hero:string, public id:string, public info:string)
    {
      this.name = name;
      this.detail = detail;
      this.price = price;
      this.hero = hero;
      this.info = info;
      this.id = id;
      
    }
  }

  

